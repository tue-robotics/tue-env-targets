- type: target
  name: hero

- type: target
  name: hero-chrony-client-pc

- type: target
  name: megasync

# RGBD server
- type: target
  name: ros-rgbd

# Laptop Battery Status
- type: target
  name: ros-upower_ros

# Middleware
- type: target
  name: tue-middleware

# RoboCup
- type: target
  name: tue-robocup-at-home

# Telegram
- type: target
  name: ros-telegram_ros

# Openpose
- type: target
  name: ros-image_recognition_openpose

# Dekstop tools
- type: target
  name: terminator
- type: system
  name: geany
- type: target
  name: chrome
