- type: target
  name: hero-demo

# RGBD server
- type: target
  name: ros-rgbd

# Laptop Battery Status
- type: target
  name: ros-upower_ros

# Middleware
- type: target
  name: tue-middleware

# challenge_demo
- type: target
  name: ros-challenge_demo

# Telegram
- type: target
  name: ros-telegram_ros

# Openpose
- type: target
  name: ros-image_recognition_openpose

# tmux
- type: target
  name: tmux

# Dekstop tools
- type: target
  name: terminator
- type: system
  name: geany
- type: target
  name: chrome
