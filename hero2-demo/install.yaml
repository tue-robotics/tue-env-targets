- type: target
  name: hero-demo

# RGBD server
- type: target
  name: ros-rgbd

# Laptop Battery Status
- type: target
  name: ros-upower_ros

# Middleware
- type: target
  name: hero-middleware

# challenge_demo
- type: target
  name: ros-challenge_demo
- type: target
  name: ros-hero_skills

# Telegram
- type: target
  name: ros-telegram_ros

# Pose estimation
- type: target
  name: cuda-setup

- type: system
  name: cuda-12-3

- type: system
  name: libcudnn9-cuda-12

- type: target
  name: ros-image_recognition_pose_estimation

# tmux
- type: target
  name: tmux

# Dekstop tools
- type: target
  name: terminator
- type: system
  name: geany
- type: target
  name: chrome
