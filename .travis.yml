os: linux
dist: xenial
language: shell
addons:
  apt:
    packages:
      - yamllint
script:
  - bash -c 'shopt -s globstar; shellcheck **/*.bash **/*.sh **/setup'
  - bash -c 'find ./ -iname '*.yaml' -or -iname '*.yml' | xargs yamllint'
