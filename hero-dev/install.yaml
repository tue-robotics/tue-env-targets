# Simulation
- type: target
  name: ros-tmc_desktop_full

# [TEMP] gazebo_plugins for melodic support
- type: target
  default:
    name: ros-gazebo_plugins # Leave here temporarily to make sure the source version is removed from the workspace
  bionic:
    name: ros-gazebo_plugins

# Middleware
- type: target
  name: tue-middleware
- type: target
  name: ros-tue_mobile_ui

# Visualization
- type: target
  name: ros-rviz

# DATA
- type: target
  name: megasync

# Diagnostics
- type: target
  name: ros-viz

# -------------------- ROBOCUP --------------------

- type: target
  name: tue-robocup-at-home
- type: target
  name: ros-hero_skills

# -------------------- ROBOTS ---------------------

- type: target
  name: hero-dashboard

- type: target
  name: hero-display

- type: target
  name: hero-user

- type: target
  name: hero-start

# Console
- type: target
  name: ros-nl_robot_console

# ED rviz plugin
- type: target
  name: ros-ed_rviz_plugins

# Handy stuff
- type: target
  name: terminator
- type: system
  name: meld

